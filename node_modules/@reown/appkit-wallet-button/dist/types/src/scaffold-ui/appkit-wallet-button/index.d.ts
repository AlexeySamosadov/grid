import { LitElement } from 'lit';
import '@reown/appkit-ui/wui-wallet-button';
import type { Wallet } from '../../utils/TypeUtil.js';
export declare class AppKitWalletButton extends LitElement {
    private unsubscribe;
    wallet: Wallet;
    private connectors;
    private caipAddress;
    private loading;
    private error;
    private ready;
    private modalLoading;
    constructor();
    disconnectedCallback(): void;
    firstUpdated(): void;
    render(): import("lit").TemplateResult<1>;
    private walletButtonTemplate;
    private externalTemplate;
    private socialTemplate;
    private emailTemplate;
}
declare global {
    interface HTMLElementTagNameMap {
        'appkit-wallet-button': AppKitWalletButton;
    }
}
